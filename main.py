from requests_heros import Super_Heroes
from yadisk import YaUploader
from stackoverflow import Stack_over

if __name__ == "__main__":
    pass
    # Задание №1
    # dict_heros = {}
    # ls_heros = ["Captain America", "Hulk", "Thanos"]
    # for hero in ls_heros:
    #     hero = Super_Heroes(hero)
    #     dict_heros[hero.name] = hero.get_intelligence()
    # print("Самый умный супергерой из списка ",
    #       ", ".join(ls_heros), ":\n", max(dict_heros), sep="")

    # Задание №2
    # path_to_file = "/home/ismoilov_m_f/Python/qwer.py"
    # with open("/home/ismoilov_m_f/Python/tokens/yadisk.txt") as f:
    #     token = f.read().split()[0]
    # uploader = YaUploader(token)
    # uploader.upload(path_to_file)

    # Задание №3
    # stack = Stack_over()
    # stack.get_tags()
